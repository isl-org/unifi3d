# @package _global_

# specify here default configuration
# order of defaults determines the order in which configs override each other
defaults:
  - _self_
  - data: sdf
  - model: encoder_decoder
  - net_encode: vqvae
  - net_denoise: unet
  - diffusion_sampler: ddpm
  - logger: null # set logger here or use command line (e.g. `python train.py logger=tensorboard`)
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

  # experiment configs allow for version control of specific hyperparameters
  # e.g. best hyperparameters for given model and datamodule
  - experiment: null

  # config for hyperparameter optimization
  - hparams_search: null

  # optional local config for machine/user specific settings
  # it's optional since it doesn't need to exist and is excluded from version control
  - optional local: default

  # debugging config (enable through command line, e.g. `python train.py debug=default)
  - debug: null

experiment_name: "train" # This will be used to initialize aim logging as well as the local folder name of the experiment.
# task name, determines output directory path
task_name: "train"
# tags to help you identify your experiments
# you can overwrite this in experiment configs
# overwrite from command line with `python train.py tags="[first_tag, second_tag]"`
tags: ["dev"]

# set False to skip model training
train: True

# evaluate on test set, using best model weights achieved during training
test: True

# simply provide checkpoint path to resume training
ckpt_path: null

# seed for random number generators in pytorch, numpy and python.random
seed: null

profiling: # Accelerate profiling configuration.
  activated: False # If True, runs the profiler. When running aim logger with profiling, wait_for_everyone hangs for accelerator. Default: False
  activities: # The list of activity groups to use in profiling. Must be one of `"cpu"`, `"xpu"`, `"mtia"`, or `"cuda"`.
    - cpu
    - cuda
  record_shapes: True # Save information about operator's input shapes.
  with_flops: True # Estimate the FLOPs (floating point operations) of specific operators.
  profile_memory: True # Analyze memory consumption.

mixed_precision: "fp16" # Mixed precision training. Choose from 'no','fp16','bf16 or 'fp8'. Default: 'no'.
